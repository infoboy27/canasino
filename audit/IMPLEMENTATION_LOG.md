# Canasino real-money implementation log

Started: 2026-09-08  
Safety rule: production remains paused; all chain tests use valueless tokens and dedicated keys.

## Baseline

- Frontend audit: `2eb59a3`; evidence/report: `8f7b4e2`.
- Game-server candidate: `audit/production-hardening` at `c73e979` (fair-randomness-v2 A.6.7; was `6287469`), based on production `e842785`. Review PR: infoboy27/canasino-gameserver#2 (base `master`) — https://github.com/infoboy27/canasino-gameserver/pull/2
- Canopy plugin candidate: `audit/canasino-financial-hardening` at `35f93f11` (fair-randomness-v2 A.2-A.4 + proposal-sim entropy + VDF fold; was `ee3b4e6e`), based on production `cd4cd495`. Review PR: infoboy27/canopy#9 (base `development`) — https://github.com/infoboy27/canopy/pull/9
- FleetWallet candidate: `audit/canasino-message-auth` at `86102d3`, based on `86fb179`.
- Baseline gates: frontend 6 unit + 9 E2E; game-server 203; plugin 177; all passing.

## Delivery gates

| # | Workstream | Status | Definition of done |
|---:|---|---|---|
| 1 | Wallet-bound authorization | IMPLEMENTED; E2E PENDING | Backend/frontend/FleetWallet protocol and cross-language proof pass; interactive extension E2E remains |
| 2 | Durable transaction reconciliation | IMPLEMENTED | Persist-before-submit outbox, exact receipt/finality, restart-safe transaction and multi-step join idempotency |
| 3 | Fair close/reveal randomness | IMPLEMENTED (Option A); INDEPENDENT REVIEW PENDING | Bets close before unpredictable entropy; deterministic settle; abort-safe refund/penalty. Plugin + game-server + isolated-node wallet->bet->payout E2E all green; VDF hardening done. Gate is now `CASINO_FAIR_RANDOMNESS_V2` (default off); external review before it is enabled anywhere real |
| 4 | Signed Poker/Domino actions | IMPLEMENTED; E2E PENDING | Every ordered action bound to player, round, turn and prior state |
| 5 | Solvency/accounting invariants | IMPLEMENTED; STRESS PENDING | Worst-case liabilities reserved; conservation and bounds proven under concurrency |
| 6 | Keys and secrets | PARTIAL | External signer/KMS interface, rotation, encryption, multisig operations |
| 7 | Reproducible observable production | PARTIAL | Immutable artifacts, SBOM, drift checks, metrics, alerts, backup/restore, kill switch |
| 8 | Valueless testnet and independent validation | PARTIAL | Wallet-to-payout E2E, fault/race/load tests and external review evidence |

## Decisions

- Authentication and transaction authorization are separate: a session proves wallet control; every economic action is additionally bound to its exact payload and a unique operation ID.
- The game-server remains fail-closed until both wallet verification and the corresponding chain operation are implemented.
- Protocol/schema changes are introduced in the Canopy branch first and consumed by the game-server/frontend only after generated types and compatibility tests pass.
- Passing unit tests never constitutes production approval.

## Evidence log

- 2026-09-08: roadmap saved; implementation started with workstream 1.
- 2026-09-08: added `CANASINO-AUTH-V1`, persistent one-use challenges/grants, BLS address/signature verification, payload binding, expiry and rate limits. Game-server 210/210 and frontend 9/9 unit tests passed. Real FleetWallet `canopy_signMessage` support is still required.
- 2026-09-08: wallet-authorized Bingo/Roulette/Domino/Poker registrations now bind the grant to `operation_id`, exact request body and `tx_hash`; the game-server verifies signer, message type, network, chain, fields and confirmation depth before applying. Duplicate operation retries are idempotent, rejected receipts stay rejected and one chain hash cannot back two operations.
- 2026-09-08: added a persist-before-submit operator outbox. Signed transaction envelopes and locally computed canonical hashes survive a lost response/restart and exact-hash receipt lookup replaces the recent-sender-window inclusion heuristic. Business-state recovery between every individual funding/join step still needs fault-injection coverage, so workstream 2 remains partial.
- 2026-09-08: game-server candidate passed 224/224 isolated tests with `--network none`; frontend passed ESLint, TypeScript, production build and 10/10 unit tests. Production remained untouched and all temporary remote test images/directories were removed.
- 2026-09-08: cloned FleetWallet at `86fb179`, added strict canonical `canopy_signMessage` approval/signing and committed candidate `86102d3`. FleetWallet passed 48/48 tests; a Noble-generated proof verified with Python `blspy` (`CROSS_LANGUAGE_VERIFY=True`).
- 2026-09-08: live node was audited read-only at height `473478`, network `1`; the configured treasury held `84,800,028` base units and the chain reported 281 prior transactions. No transaction was submitted. This node is not considered valueless.
- 2026-09-08: Poker/Domino actions now consume a one-use wallet grant bound to UUID, player, round, sequence and SHA-256 of the exact prior public state. The state comparison and action application occur under one lock; stale/replayed state is rejected. Frontend helpers produce the same payload/resource contract.
- 2026-09-08: roulette now transfers a conservative worst-case liability reserve from treasury to escrow atomically with each accepted bet. Underfunded bets fail before acceptance; expiry refunds stakes and releases reserves. Bingo/Poker/Domino remain escrow-bounded. Plugin suite passed 177/177.
- 2026-09-08: added a verified external BLS signer interface over a local Unix socket. Real-money mode requires an external signer plus configured operator public identity and refuses SQLite operator-key fallback. Rotation/multisig governance remain pending.
- 2026-09-08: added backend chain-write kill switch (off by default for a real RPC), capability/Prometheus gauges, immutable build metadata, checksummed SQLite backup verification and removal of automatic production Git commits/pushes. Game-server suite passed 232/232 in an isolated container.
- 2026-09-08: launched a separate clean Canopy node using image digest `sha256:2340361...`, internal Docker network and isolated `/tmp/canasino-valueless-audit-20260908` data. It produced blocks with a new validator and Python plugin query returned HTTP 200. No host ports or production mounts were used; no transactions were submitted.
- 2026-09-08: frontend passed ESLint, TypeScript, 11/11 unit tests and production build. All changes remain local candidates; production is still paused and unchanged.
- 2026-09-08: fault injection now covers the business-level `fund -> join/bet` boundary. A durable custodial-join saga stores the generated key and exact payload before funding, resumes the same outbox operations after a lost accepted response, and applies local membership once. Bingo, Roulette, Domino and Poker each proved one fund and one join after restart; full game-server suite passed 233/233 with no network.
- 2026-09-08: inspected the Canopy consensus/plugin boundary for fair randomness. Canopy commits block hashes and VDF data, but `PluginBeginRequest` currently exposes only `height`; the plugin cannot authenticate a historical hash supplied by the operator. Workstream 3 therefore remains blocking pending an upstream-compatible FSM-to-plugin entropy field plus close-height/bond lifecycle tests.
- 2026-09-08: operational controls now expose fairness and pending-saga gauges, ship six validated Prometheus safety rules, and include a fail-closed runtime verifier for expected OCI revision/image ID plus health. `promtool` accepted all six rules; shell syntax passed; the isolated game-server suite remained 233/233. Alert delivery integration, SBOM/signing and an off-box restore rehearsal remain for workstream 7/8.
- 2026-09-09: workstream 3 Option A chosen and specified (audit/specs/fair-randomness-v2.md sections A.1-A.7): consensus block-hash entropy delivered FSM->plugin, folded over a window of finalized blocks fixed after a round-close transaction, VDF-ready. Entropy source recommendation: fold ENTROPY_WINDOW_BLOCKS consecutive finalized hashes rather than one (grinding then needs an unbroken proposer run), with a reserved `vdf_output` field for later. First foundation commit on the Canopy branch: `PluginBeginRequest` gains `last_block_hash` (populated) and `vdf_output` (reserved); `BeginBlock(lastBlockHash []byte)` is fed the applying block header's `LastBlockHash`; the plugin persists `hash||vdf` to a retained ledger (prefix 0x76) and folds a window at settle, failing closed on any gap or on a missing hash post-genesis. Proto regenerated with the pinned toolchain (protoc 29.3 / protoc-gen-go v1.36.6 / grpcio-tools 1.60.x), minimal diff. Verified in local Docker (`--network none` where applicable): `go build ./...`, `go test ./fsm/ ./lib/` green; plugin suite 182/182. No production contact; the game candidate repos and the isolated node are untouched. Round-close transaction, per-game bond + settle/expire rewrites, replay test and isolated-node E2E still pending; `fairRandomnessV2` stays false.
- 2026-09-09: workstream 3 Option A A.4 landed on the Canopy branch (`b7375e72`). Every money game (Bingo/Roulette/Domino/Poker) now: escrows `operator_bond` into a per-round, per-game salted sub-account at open; requires an operator-only `MessageClose<Game>` that stamps the entropy window `[close_height+8, +15]` before any of those blocks exist; settles only once `current_height > entropy_end`, folding the window from the plugin's own FSM-fed ledger (fail-closed on any gap) and replaying from `SHA256(revealed_secret || entropy || round_id)`; returns the bond on settle and slashes it to the treasury on expire. Expire now also frees a closed-but-unsettled round after `entropy_end + 720`. Join/bet reject the operator playing its own round; expire fails closed if the treasury is a participant, so the bond leg never mis-accounts. `tx.proto` gained `operator_bond` on the 4 opens, 4 `MessageClose<Game>`, and `close_height/entropy_start/entropy_end/operator_bond` on the 4 round records; `tx_pb2.py` regenerated with the pinned toolchain (protoc 29.3 / grpcio-tools 1.60.1), additive-only. New `tests/test_fair_randomness_v2.py` drives open->join->close->advance->settle and independently recomputes the folded entropy, seed and outcome, and proves grind-resistance (one changed window hash changes the seed), fail-closed on a missing window block, and bond slash on an unsettled closed round. Verified in an isolated container (`--network none`, Python 3.12, protobuf 4.25): full plugin suite 196/196. No Go changes. `audit/patches/plugin.patch` regenerated. Game-server consumption of the new lifecycle and the wallet->bet->payout E2E on the isolated node remain; `fairRandomnessV2` stays false.
- 2026-09-09: workstream 3 A.6.6/A.6.7 closed the loop. Game-server candidate (`f8fc1d1`): every manager drives `open -> close -> wait(entropy_end+1) -> settle`; `gameserver/fairness.py` reconstructs the plugin's replay seed from public finalized blocks and is now byte-exact with the plugin -- a first pass concatenated the seed parts without the 4-byte length prefixes `derive_seed` applies, so the winner the game-server computed disagreed with the on-chain payout ~50% of the time (caught only by repeating the E2E). `derive_final_seed` now calls `engine.rng.derive_seed`; a parity test plus a frozen known-answer vector guard against future drift. Managers persist and restore the close/entropy boundary; bingo/domino/poker only build their engine and reveal cards once the outcome seed exists. Canopy candidate (`a113484f`): `BeginBlock` resolves the one empty `LastBlockHash` case (proposal simulation's skeletal header) from the FSM's indexed predecessor, so the plugin stays fail-closed without breaking block production. Isolated valueless testnet rebuilt with 250 ms phase timers and a locally-built `canasino-canopy-a67-e2e` image. `audit/testnet/e2e_wallet_bet_payout.py`: real `MessageCloseRoulette` receipt, waits through the window, asserts the exact settle receipt height and that the plugin-computed winner's on-chain balance rose. Ran 6x back to back post-fix -- spins 4/7/15/20/35, payout matched the computed winner every run. Full game-server suite 240/240 and full plugin suite 196/196 in containers; `go build ./fsm ./lib` + `TestBeginBlock`/`TestApplyBlock`/`TestEndBlock` green (the lone `TestCheckSignatureRejectsNonCanonicalPublicKey` failure is a no-cgo secp256k1 limitation of the alpine test image, unrelated). `plugin.patch`/`gameserver.patch` regenerated. Remaining before `fairRandomnessV2`/`realMoneyEnabled`: VDF hardening (reserved `vdf_output` wired), and independent replay + review of the whole close/reveal path.
- 2026-09-09: workstream 3 VDF hardening. `BeginBlock` now also forwards `LoadBlock(H-1).BlockHeader.Vdf.Output` -- the committed predecessor's own consensus VDF (over the block before it) -- in `PluginBeginRequest.vdf_output`. Both entropy inputs come from the FSM's committed index, so the ledger value `hash||vdf` is identical for the proposer's block simulation (skeletal header, neither field set) and every validator; the applying header's own VDF is stapled after the proposer computes the state root and is deliberately never used. The plugin already folds the whole ledger value, so a full-window grind now also has to beat an honest non-parallelizable delay on every block. `CANASINO_REQUIRE_CONSENSUS_VDF` (plugin + game-server, off by default) makes settle fail closed unless every window block carried a VDF, and the game-server refuses such a window up front. `CanopyBridge.consensus_entropy_value` folds `hash || hex-decoded blockHeader.vdf.output` to match `encode_consensus_entropy` byte for byte; parity test + frozen vector updated. New plugin tests: VDF persisted with the hash, the VDF term changes the fold, require-mode fails closed on a hash-only or partly-VDF-less window and settles on a full one. Verified in isolated containers: plugin suite 201/201, game-server 241/241, `go build ./fsm ./lib` + `TestBeginBlock`/`TestApplyBlock`/`TestEndBlock` green. Isolated testnet rebuilt on a fresh chain (VDF present on every block); `e2e_wallet_bet_payout.py` passed 5/5 with the VDF folded (spins 7/11/13/15/22, payout matched the computed winner every run). Canopy candidate now `35f93f11`; game-server candidate now `c73e979`. Independent review of the close/reveal path is the last gate before `fairRandomnessV2`.
- 2026-09-09: workstream 3 candidate branches pushed and opened for independent review. Canopy consensus + plugin: infoboy27/canopy#9 (branch `audit/canasino-financial-hardening` -> base `development`, 6 commits / 26 files) — https://github.com/infoboy27/canopy/pull/9. Game-server: infoboy27/canasino-gameserver#2 (branch `audit/production-hardening` -> base `master`, 7 commits / 38 files) — https://github.com/infoboy27/canasino-gameserver/pull/2. Both open, `mergeable` with no conflicts (CI pending); neither is merged. Both PR descriptions state that nothing goes live — the plugin stays fail-closed and the game-server's `fair_randomness_v2` flag remains hard-coded `False`. The canopy branch is also on the SSH `origin` (`ssh://ubuntu@163.245.207.38:1600/.../canopy`); the GitHub push targets the public mirror `infoboy27/canopy` (a fork whose `development` head is the audited baseline `cd4cd495`). The audit evidence repo (this file, specs, patches, testnet) is on `infoboy27/canasino` `main` at `7622bb8`+.
- 2026-09-09: FleetWallet candidate (`audit/canasino-message-auth` @ `86102d3`) packaged for local install -- `dist/fleetwallet-extension.zip` (MV3, 76 files, sha256 `6c8a04992c042bcccbbea98b56f654fb317d2886cc4f711e671796c3e89ca25c`), built straight from `extension/` (no bundler; deps vendored under `lib/vendor`). `node --test tests/*.test.js` 48/48; `scripts/scan-compromised-packages.py` clean. Zip is a build artifact (gitignored, as in the Makefile). Local install: unzip and load the folder as an unpacked extension (Chrome: chrome://extensions -> Developer mode -> Load unpacked; Firefox: about:debugging -> Load Temporary Add-on -> pick manifest.json). Point it at the local game-server + isolated Canopy RPC via the wallet's chain settings. Interactive `canopy_signMessage` -> bingo/roulette/domino/poker join/action -> settle in a real browser is still the workstream 1 / 8 E2E to run.
- 2026-09-09: fair-randomness-v2 gate is no longer hard-coded. `gameserver/app.py` reads `CASINO_FAIR_RANDOMNESS_V2` (default off) -- a dev/valueless deployment opts in without a code change; every other deployment stays fail-closed pending independent review. Real money still additionally needs `CASINO_REAL_MONEY_ENABLED` + `CASINO_CHAIN_WRITES_ENABLED` + `CASINO_SIGNER_SOCKET`. New test asserts the opt-in flips `fairRandomnessV2` in `/capabilities` and `/metrics` but not `realMoneyEnabled`. Game-server suite 242/242. Branch `audit/production-hardening` now at `d542bae`; PR infoboy27/canasino-gameserver#2 updated.
